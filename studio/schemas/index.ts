import blockContent from './blockContent'
import blogPost from './blogPost'
import galleryImage from './galleryImage'
import expPost from './expPost'

export const schemaTypes = [blockContent, blogPost, galleryImage, expPost]
